{
  "name": "dsh-dle",
  "version": "1.0.0",
  "description": "DSH DLE — DeepSeek Harness Desktop Lite Edition: thin Electron shell (tray + notifications + security) sharing the dev ~/.dsh profile",
  "main": "main.js",
  "author": "DSH DLE",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --win",
    "test": "node --check main.js && node --check scripts/session-watcher.js && node --check scripts/mux-watcher.js && node --check scripts/icon-utils.js && node --check scripts/generate-whale-icon.js && node --check scripts/make-icon-from-png.js && node --check scripts/sprite-to-png.js && node --check scripts/port-utils.js && node tests/port-utils.test.js && node tests/session-watcher.test.js"
  },
  "devDependencies": {
    "electron": "^41.0.0",
    "electron-builder": "^26.0.0",
    "eslint": "^9.0.0",
    "globals": "^14.0.0"
  },
  "build": {
    "appId": "com.deepseek.dshdle",
    "productName": "DSH DLE",
    "artifactName": "DSH-DLE-Setup-${version}.${ext}",
    "electronDist": "node_modules/electron/dist",
    "electronLanguages": [
      "en-US",
      "zh-CN"
    ],
    "files": [
      "main.js",
      "scripts/port-utils.js",
      "assets/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "scripts/session-watcher.js",
        "to": "scripts/session-watcher.js"
      },
      {
        "from": "scripts/mux-watcher.js",
        "to": "scripts/mux-watcher.js"
      }
    ],
    "directories": {
      "output": "release"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "assets/icon.ico"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "createDesktopShortcut": true,
      "shortcutName": "DSH DLE",
      "deleteAppDataOnUninstall": false
    }
  },
  "allowScripts": {
    "electron@41.10.5": true
  }
}
